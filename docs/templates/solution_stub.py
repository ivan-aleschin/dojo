"""Шаблон ЗАДАЧИ (стаб). Копируется в <категория>/<паттерн>/<NN_имя>/solution.py.

Правила: доктесты уже написаны и падают, задача — сделать их зелёными.
Тело функции не трогаем до тех пор, пока не проговорили идею и сложность вслух.

Проверка одного файла:
    python -m doctest <путь>/solution.py -v
Весь репозиторий:
    pytest
"""


def solve(nums: list[int], target: int) -> int:
    """Одна строка: что делает функция.

    Паттерн: <название>.
    Время O(?), память O(?).

    >>> solve([1, 2, 3], 2)
    2
    >>> solve([], 0)
    0
    """
    raise NotImplementedError("твоя очередь")
