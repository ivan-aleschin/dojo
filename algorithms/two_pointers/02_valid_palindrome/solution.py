def is_palindrome(s: str) -> bool:
    """True, если s — палиндром, если учитывать ТОЛЬКО буквы и цифры и игнорировать регистр.

    Задача на паттерн Two Pointers (схема "навстречу").
    Подсказки — в algorithms/two_pointers/README.md. Реализуй сам, чтобы доктесты прошли.

    Ожидаемая сложность: время O(n), память O(1).

    >>> is_palindrome("A man, a plan, a canal: Panama")
    True
    >>> is_palindrome("race a car")
    False
    >>> is_palindrome("")
    True
    >>> is_palindrome(".,")
    True
    >>> is_palindrome("0P")
    False
    """
    raise NotImplementedError("твоя очередь: реализуй через два указателя")
